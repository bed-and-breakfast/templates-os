export function add(first: number, second: number) {
    const a = 'a';

    return first + second;
}

export function multiply(first: number, second: number) {
    const a = 'a';

    return first * second;
}
